// Using imperative coding, implement your own map function
// the map function should take two arguments:
// array to map over, and a callback function to execute on it
// it should return a new mapped array, and not mutate it

const myMap = (array, fn) => {
  return null;
};

module.exports = myMap;
